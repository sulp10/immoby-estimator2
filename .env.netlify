# Netlify environment variables (server-side)
# DO NOT COMMIT REAL SECRETS
# Copy these into Netlify → Site settings → Build & deploy → Environment

# Server-side secrets (used by Next.js API routes). These are NOT exposed to the browser.
GOOGLE_MAPS_API_KEY=
AIRROI_API_KEY=
DATABASE_URL=postgresql://neondb_owner:npg_3gwDLhXNF4qk@ep-frosty-sun-ad3xrvdm-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Optional public (client-side) variables — only if strictly needed by client scripts
NEXT_PUBLIC_API_BASE_URL=https://immobyestimator.netlify.app

# Notes:
# - Variables without the NEXT_PUBLIC_ prefix remain server-side only
# - Build settings on Netlify: Build command = npm run build ; Publish directory = .next
# - After setting variables, trigger “Clear cache and deploy” on Netlify
# - Restrict the Google Maps key to your Netlify domain in Google Cloud Console for security