# Local development environment variables
# DO NOT COMMIT REAL SECRETS
# This file can be used for local testing. Copy values from your secure storage.

# Server-side secrets (Next.js API routes)
GOOGLE_MAPS_API_KEY=
AIRROI_API_KEY=
DATABASE_URL=

# Optional public (client-side) variables — only if strictly needed by client scripts
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3002

# Notes:
# - Variables without the NEXT_PUBLIC_ prefix remain server-side only
# - Run locally: npx next dev -p 8080
# - Build: npm run build